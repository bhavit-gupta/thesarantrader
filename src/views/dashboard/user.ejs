<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - The Saran Trader</title>
    <link rel="stylesheet" href="/stylesheets/output.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <style>
        @keyframes pulse-live {

            0%,
            100% {
                opacity: 1;
            }

            50% {
                opacity: 0.4;
            }
        }

        .live-pulse {
            animation: pulse-live 1.5s ease-in-out infinite;
        }

        @keyframes float {

            0%,
            100% {
                transform: translateY(0px);
            }

            50% {
                transform: translateY(-6px);
            }
        }

        .float-anim {
            animation: float 3s ease-in-out infinite;
        }
    </style>
</head>

<body class="font-sans antialiased text-slate-600 bg-slate-50 min-h-screen flex flex-col">

    <%- include('../partials/navbar') %>

        <main class="flex-grow pt-24 pb-12">
            <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">

                <!-- Welcome Header -->
                <div class="mb-8">
                    <h1 class="text-3xl lg:text-4xl font-bold text-slate-800">
                        Welcome back, <span
                            class="text-transparent bg-clip-text bg-gradient-to-r from-blue-600 to-blue-400">
                            <%= user.name %>
                        </span> 👋
                    </h1>
                    <p class="text-slate-500 mt-2 text-lg">Here's what's happening on your trading journey.</p>
                </div>

                <!-- Top Row: Join Live Card + Quick Stats -->
                <div class="grid grid-cols-1 lg:grid-cols-3 gap-6 mb-8">

                    <!-- Live Sessions List -->
                    <div class="lg:col-span-2 space-y-4">
                        <% let activeSessions=0; courses.forEach(course=> {
                            if (liveSessions[course.id] && liveSessions[course.id].isLive) {
                            activeSessions++;
                            }
                            });
                            %>

                            <!-- Active Sessions -->
                            <% if (activeSessions> 0) { %>
                                <% courses.forEach(course=> {
                                    const session = liveSessions[course.id];
                                    if (session && session.isLive) {
                                    %>
                                    <div
                                        class="relative overflow-hidden rounded-2xl border border-green-200 bg-white shadow-md">
                                        <div class="absolute inset-0 bg-green-50/30 pointer-events-none"></div>
                                        <div class="absolute top-0 right-0 p-20 bg-green-400/10 rounded-full blur-3xl">
                                        </div>

                                        <div class="relative p-6 flex flex-col sm:flex-row items-center gap-6">
                                            <!-- Icon -->
                                            <div class="relative">
                                                <div
                                                    class="w-16 h-16 rounded-xl bg-<%= course.iconBg %> flex items-center justify-center text-3xl shrink-0 text-<%= course.iconColor %>">
                                                    <%= course.icon %>
                                                </div>
                                                <span
                                                    class="absolute -top-1 -right-1 w-4 h-4 rounded-full bg-green-500 border-2 border-white animate-pulse"></span>
                                            </div>

                                            <!-- Content -->
                                            <div class="flex-grow text-center sm:text-left">
                                                <div
                                                    class="flex items-center justify-center sm:justify-start gap-2 mb-1">
                                                    <span
                                                        class="px-2 py-0.5 rounded text-xs font-bold bg-green-100 text-green-700 uppercase tracking-wide">Live
                                                        Now</span>
                                                    <span class="text-xs text-slate-400 font-mono"
                                                        id="user-timer-<%= course.id %>"
                                                        data-start="<%= session.startTime %>">00:00:00</span>
                                                </div>
                                                <h2 class="text-xl font-bold text-slate-800">
                                                    <%= course.title %>
                                                </h2>
                                                <p class="text-slate-500 text-sm">Join the ongoing session with Kundan
                                                    Sir.</p>
                                            </div>

                                            <!-- Action -->
                                            <a href="<%= course.liveLink || '#' %>" target="_blank"
                                                class="inline-flex items-center gap-2 px-6 py-3 rounded-full bg-green-600 text-white font-bold text-sm shadow-lg shadow-green-500/30 hover:bg-green-700 hover:-translate-y-0.5 transition-all whitespace-nowrap">
                                                <i class="fa-solid fa-play"></i> Join Class
                                            </a>
                                        </div>
                                    </div>
                                    <% } }); %>
                                        <% } else { %>
                                            <!-- No Live Sessions Placeholder -->
                                            <div
                                                class="relative overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm p-8 text-center">
                                                <div
                                                    class="w-16 h-16 rounded-2xl bg-slate-100 flex items-center justify-center text-3xl mx-auto mb-4 text-slate-400">
                                                    😴
                                                </div>
                                                <h2 class="text-xl font-bold text-slate-800 mb-2">No Live Sessions
                                                    Active</h2>
                                                <p class="text-slate-500 max-w-sm mx-auto">
                                                    There are no live classes happening right now. Check back later or
                                                    browse your recorded courses.
                                                </p>
                                            </div>
                                            <% } %>
                    </div>

                    <!-- Quick Stats -->
                    <div class="space-y-4">
                        <div
                            class="bg-white rounded-2xl border border-slate-200 p-5 shadow-sm hover:shadow-md transition-shadow">
                            <div class="flex items-center gap-4">
                                <div
                                    class="w-12 h-12 rounded-xl bg-blue-100 flex items-center justify-center text-blue-600 text-xl">
                                    📚</div>
                                <div>
                                    <p class="text-2xl font-bold text-slate-800">0</p>
                                    <p class="text-sm text-slate-500">Enrolled Courses</p>
                                </div>
                            </div>
                        </div>
                        <div
                            class="bg-white rounded-2xl border border-slate-200 p-5 shadow-sm hover:shadow-md transition-shadow">
                            <div class="flex items-center gap-4">
                                <div
                                    class="w-12 h-12 rounded-xl bg-green-100 flex items-center justify-center text-green-600 text-xl">
                                    🎯</div>
                                <div>
                                    <p class="text-2xl font-bold text-slate-800">0</p>
                                    <p class="text-sm text-slate-500">Live Sessions Attended</p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- My Courses Section -->
                <div class="mb-8">
                    <div class="flex items-center justify-between mb-6">
                        <h2 class="text-2xl font-bold text-slate-800">My Courses</h2>
                        <a href="/courses"
                            class="text-sm text-blue-600 font-semibold hover:text-blue-700 transition-colors flex items-center gap-1">
                            Browse All <i class="fa-solid fa-arrow-right text-xs"></i>
                        </a>
                    </div>
                    <!-- Empty State -->
                    <div class="bg-white rounded-2xl border border-dashed border-slate-300 p-12 text-center">
                        <div class="text-5xl mb-4">🎓</div>
                        <h3 class="text-xl font-bold text-slate-800 mb-2">No courses yet</h3>
                        <p class="text-slate-500 mb-6 max-w-md mx-auto">Start your trading journey by enrolling in one
                            of our expert-crafted courses.</p>
                        <a href="/courses"
                            class="inline-flex items-center gap-2 px-6 py-3 rounded-full bg-blue-600 text-white font-semibold text-sm shadow-lg shadow-blue-500/25 hover:bg-blue-700 hover:-translate-y-0.5 transition-all">
                            <i class="fa-solid fa-graduation-cap"></i> Explore Courses
                        </a>
                    </div>
                </div>

                <!-- Account Details Section -->
                <div class="bg-white rounded-2xl border border-slate-200 shadow-sm overflow-hidden">
                    <div class="px-8 py-5 border-b border-slate-100 flex items-center gap-3">
                        <div class="w-10 h-10 rounded-xl bg-slate-100 flex items-center justify-center text-slate-500">
                            <i class="fa-solid fa-user"></i>
                        </div>
                        <h2 class="text-lg font-bold text-slate-800">Account Details</h2>
                    </div>
                    <div class="p-8">
                        <div class="grid grid-cols-1 sm:grid-cols-2 gap-6">
                            <div>
                                <label
                                    class="block text-xs font-semibold text-slate-400 uppercase tracking-wider mb-1">Name</label>
                                <p class="text-slate-800 font-medium text-lg">
                                    <%= user.name %>
                                </p>
                            </div>
                            <div>
                                <label
                                    class="block text-xs font-semibold text-slate-400 uppercase tracking-wider mb-1">Username</label>
                                <p class="text-slate-800 font-medium text-lg">@<%= user.username %>
                                </p>
                            </div>
                            <div>
                                <label
                                    class="block text-xs font-semibold text-slate-400 uppercase tracking-wider mb-1">Email</label>
                                <p class="text-slate-800 font-medium text-lg">
                                    <%= user.email %>
                                </p>
                            </div>
                            <div>
                                <label
                                    class="block text-xs font-semibold text-slate-400 uppercase tracking-wider mb-1">Member
                                    Since</label>
                                <p class="text-slate-800 font-medium text-lg">2026</p>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Share Your Experience Section -->
                <div class="bg-white rounded-2xl border border-slate-200 shadow-sm overflow-hidden mt-8">
                    <div class="px-8 py-5 border-b border-slate-100 flex items-center gap-3">
                        <div
                            class="w-10 h-10 rounded-xl bg-yellow-100 flex items-center justify-center text-yellow-600">
                            <i class="fa-solid fa-star"></i>
                        </div>
                        <h2 class="text-lg font-bold text-slate-800">Share Your Experience</h2>
                    </div>
                    <div class="p-8">
                        <!-- Existing Testimonials Display -->
                        <div id="existing-testimonial" class="hidden mb-6">
                            <!-- Testimonials will be loaded here dynamically -->
                        </div>

                        <!-- Testimonial Form -->
                        <form id="testimonial-form">
                            <div class="space-y-6">
                                <!-- Star Rating -->
                                <div>
                                    <label class="block text-sm font-semibold text-slate-700 mb-3">
                                        Rate Your Experience <span class="text-red-500">*</span>
                                    </label>
                                    <div class="flex gap-2">
                                        <button type="button"
                                            class="star-btn text-2xl text-slate-300 hover:text-yellow-400 transition-colors focus:outline-none"
                                            data-rating="1">
                                            <i class="fa-solid fa-star"></i>
                                        </button>
                                        <button type="button"
                                            class="star-btn text-2xl text-slate-300 hover:text-yellow-400 transition-colors focus:outline-none"
                                            data-rating="2">
                                            <i class="fa-solid fa-star"></i>
                                        </button>
                                        <button type="button"
                                            class="star-btn text-2xl text-slate-300 hover:text-yellow-400 transition-colors focus:outline-none"
                                            data-rating="3">
                                            <i class="fa-solid fa-star"></i>
                                        </button>
                                        <button type="button"
                                            class="star-btn text-2xl text-slate-300 hover:text-yellow-400 transition-colors focus:outline-none"
                                            data-rating="4">
                                            <i class="fa-solid fa-star"></i>
                                        </button>
                                        <button type="button"
                                            class="star-btn text-2xl text-slate-300 hover:text-yellow-400 transition-colors focus:outline-none"
                                            data-rating="5">
                                            <i class="fa-solid fa-star"></i>
                                        </button>
                                    </div>
                                    <input type="hidden" id="rating-input" name="rating" required>
                                </div>

                                <!-- Role/Profession -->
                                <div>
                                    <label for="user-role" class="block text-sm font-semibold text-slate-700 mb-2">
                                        Your Role/Profession
                                    </label>
                                    <input type="text" id="user-role" name="userRole"
                                        placeholder="e.g., Full-Time Trader, Student, Working Professional"
                                        class="w-full px-4 py-3 rounded-xl border border-slate-200 focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20 outline-none transition-all">
                                </div>

                                <!-- Testimonial Message -->
                                <div>
                                    <label for="testimonial-message"
                                        class="block text-sm font-semibold text-slate-700 mb-2">
                                        Your Testimonial <span class="text-red-500">*</span>
                                    </label>
                                    <textarea id="testimonial-message" name="message" rows="4" required maxlength="500"
                                        placeholder="Share your experience with our courses and how they've helped you..."
                                        class="w-full px-4 py-3 rounded-xl border border-slate-200 focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20 outline-none transition-all resize-none"></textarea>
                                    <div class="flex justify-between items-center mt-2">
                                        <span class="text-xs text-slate-400">Maximum 500 characters</span>
                                        <span id="char-count" class="text-xs text-slate-500 font-medium">0 / 500</span>
                                    </div>
                                </div>

                                <!-- Feedback Message -->
                                <div id="testimonial-feedback" class="hidden p-4 rounded-xl"></div>

                                <!-- Submit Button -->
                                <button type="submit" id="submit-testimonial-btn"
                                    class="w-full px-6 py-3 rounded-xl bg-gradient-to-r from-blue-600 to-blue-500 text-white font-semibold shadow-lg shadow-blue-500/25 hover:shadow-blue-500/40 hover:-translate-y-0.5 transition-all flex items-center justify-center gap-2">
                                    <i class="fa-solid fa-paper-plane"></i>
                                    Submit Testimonial
                                </button>
                            </div>
                        </form>
                    </div>
                </div>

            </div>
        </main>

        <%- include('../partials/footer') %>

            <script src="/javascripts/main.js"></script>
            <script src="/javascripts/user-dashboard.js"></script>
</body>

</html>